# ─── SaaS Platform Core (multi-tenant) ─────────────────────────────
# Runtime mode for containerized deployments:
#   all (default), web, telegram, scheduler, job
APP_MODE=all
# Used only when APP_MODE=job (example: overdue_rent)
JOB_ID=

# Set to 1 to enable tenant-aware routing/auth/integration handling.
PLATFORM_MULTI_TENANT=1
# Recommended for production multi-tenant deployments.
DATABASE_URL=postgresql://user:password@localhost:5432/property_saas
# Optional fallback store file when DATABASE_URL is not provided.
PLATFORM_STORE_PATH=./platform-store.json
# 32-byte key (hex/base64/utf8) used to encrypt tenant integration credentials at rest.
PLATFORM_ENCRYPTION_KEY=
# Bootstrap platform-owner login for SaaS admin API (/api/v2/auth/*).
PLATFORM_OWNER_EMAIL=owner@example.com
PLATFORM_OWNER_PASSWORD=ChangeMe123!
# Bootstrap/default tenant metadata.
DEFAULT_TENANT_SLUG=legacy-default
DEFAULT_TENANT_NAME=Legacy Default Company
DEFAULT_TENANT_TIMEZONE=America/Los_Angeles
DEFAULT_TENANT_LOCALE=en-US
DEFAULT_TENANT_CURRENCY=USD

# ─── ERPNext / PropMS ────────────────────────────────
# Base URL of your ERPNext instance (no trailing slash)
ERPNEXT_BASE_URL=https://erpnext.yourdomain.com
# ERPNext API key + secret — generate in ERPNext → User → API Access
ERPNEXT_API_KEY=your_erpnext_api_key
ERPNEXT_API_SECRET=your_erpnext_api_secret

# ─── OpenAI ──────────────────────────────────────────
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o

# ─── Telegram Bot ────────────────────────────────────
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_from_botfather
# Comma-separated Telegram user IDs allowed to use the bot (landlord/managers only)
TELEGRAM_ALLOWED_USER_IDS=123456789,987654321

# ─── Webhook Server ──────────────────────────────────
WEBHOOK_PORT=3000
# Shared secret used to validate incoming ERPNext webhook signatures.
# Set the same value in ERPNext → Integrations → Webhooks → Secret.
WEBHOOK_SECRET=your_webhook_shared_secret
# Publicly reachable URL of this server (behind a reverse proxy / ngrok in dev)
WEBHOOK_BASE_URL=https://your-server.example.com

# ─── Tenant Portal – Stripe Payments ────────────────
# Enables the "Pay Now" button on rent invoices in the tenant portal.
# Get keys at https://dashboard.stripe.com/apikeys
# Leave blank to install Stripe Settings without activating payment collection.
STRIPE_PUBLISHABLE_KEY=pk_live_your_publishable_key
STRIPE_SECRET_KEY=sk_live_your_secret_key
# GL account to credit when Stripe payments are received (instance-specific).
# Find it in ERPNext → Chart of Accounts.  Default works for standard installs.
STRIPE_PAYMENT_ACCOUNT=Debtors - LC

# ─── SMS – Twilio (required) ──────────────────────────
# ERPNext has no outbound SMS API; Twilio handles all tenant text messages.
# Sign up at https://twilio.com, buy a number, and fill in the values below.
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_FROM_NUMBER=+15550001234

# ─── Hemlane Replacement: BoldSign (lease e-signatures) ──────
# BoldSign is used for e-signatures ($5–99/mo, free tier available).
# 1. Create account at https://app.boldsign.com
# 2. Upload each lease PDF as a template with signer roles: Tenant (index 1), Landlord (index 2)
#    Tag every pre-fill field in the document with a merge variable name (see field list below).
# 3. Copy each Template ID into the matching variable below
# 4. Generate an API key at app.boldsign.com → API Management
# 5. Create a webhook at app.boldsign.com → API Management → Webhooks
#    pointing to: https://your-server.example.com/webhooks/boldsign/completed
#    Copy the webhook signing secret below.
BOLDSIGN_API_KEY=your_boldsign_api_key
BOLDSIGN_WEBHOOK_SECRET=your_boldsign_webhook_signing_secret

# One template per state × document type.
# The correct template is selected automatically from the property's State field in ERPNext.
BOLDSIGN_TEMPLATE_OH_LEASE=your_ohio_lease_template_id
BOLDSIGN_TEMPLATE_OH_RENEWAL=your_ohio_renewal_template_id
BOLDSIGN_TEMPLATE_NC_LEASE=your_north_carolina_lease_template_id
BOLDSIGN_TEMPLATE_NC_RENEWAL=your_north_carolina_renewal_template_id
# Legacy fallback — used if no state-specific template is found (optional).
BOLDSIGN_TEMPLATE_ID=

# Landlord details pre-filled into every document (counter-signer).
LANDLORD_NAME=Your Name
LANDLORD_EMAIL=landlord@example.com
LANDLORD_PHONE=+15550001234
LANDLORD_ADDRESS=123 Owner Street, City ST 00000

# BoldSign merge field names used in the templates (tag your PDF fields with these IDs):
#   landlord_name, landlord_email, landlord_phone, landlord_address
#   tenant_name, tenant_email, tenant_phone
#   unit_address, unit_city, unit_state, unit_zip
#   start_date, end_date, monthly_rent, security_deposit
#   notice_period, late_fee_grace_days, late_fee_amount

# ─── Hemlane Replacement: TransUnion SmartMove (tenant screening) ─
# Create a landlord account at https://www.mysmartmove.com
# and generate an API key under Developer Settings.
SMARTMOVE_API_KEY=your_smartmove_api_key

# ─── Late Fee Auto-Charging ───────────────────────────
# Set to "1" to automatically submit late fee invoices (immediately visible on tenant portal).
# Leave as "0" (default) to create them as drafts for accountant review before submission.
LATE_FEE_AUTO_SUBMIT=0

# ─── Reports ─────────────────────────────────────────
# Where to deliver weekly reports: "telegram" | "email" | "gdrive"
REPORT_DELIVERY=telegram
REPORT_GOOGLE_DRIVE_FOLDER_ID=
REPORT_EMAIL_TO=landlord@example.com

# ─── Logging ─────────────────────────────────────────
LOG_LEVEL=info
